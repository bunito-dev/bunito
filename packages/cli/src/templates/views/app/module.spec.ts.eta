import { describe, expect, test } from 'bun:test';
import { <%= it.moduleClass %> } from '@app<% if (!it.root) { %>s/<%= it.name %><% } else { %><% } %>';

describe('<%= it.moduleClass %>', () => {
  test.todo('add integration tests', () => {
    expect(<%= it.moduleClass %>).toBeDefined();
  });
});
