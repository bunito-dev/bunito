{
  "name": "bunito",
  "private": true,
  "version": "0.0.5",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "coverage": "bun test --coverage",
    "lint": "biome check",
    "format": "biome format",
    "typecheck": "tsc --noEmit"
  },
  "workspaces": {
    "packages": [
      "example",
      "packages/*"
    ],
    "catalog": {
      "zod": "^4.3.6"
    }
  },
  "dependencies": {},
  "devDependencies": {
    "@biomejs/biome": "2.4.11",
    "typescript": "^6.0.2"
  },
  "engines": {
    "bun": ">=1.3.11"
  }
}
